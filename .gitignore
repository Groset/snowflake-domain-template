# Environment / secrets
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Assembled deploy bundles — regenerated each deploy from source.
# See ai/context/deployment.md.
deploy-prd/
deploy-dev/

# Python (for any ai/session/ scripts)
__pycache__/
*.pyc
.venv/
venv/

# Local-only ai/session/ working files (uncomment if needed per repo)
# ai/session/*.local.*

# Local notes (uncomment if needed per repo)
# *.local.md
